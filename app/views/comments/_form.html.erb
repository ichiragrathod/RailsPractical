<%= form_for [commentable, Comment.new] do |f| %>
	<%= f.text_area :content, placeholder:"Add Comment",:required => true, class:"form-control"%>
	<br>
	<%= f.submit "Add Comment", class:"btn btn-primary"%>
<% end %>
